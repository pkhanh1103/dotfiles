alias ll='ls -l'
alias llt='ls -lt'
alias lla='ls -la'
alias lls='ls -lSh'

alias reload='source ~/.zshrc'
#alias reload='source ~/.bashrc'

if [ -f $HOME/.privaterc ]; then
    . $HOME/.privaterc
fi
